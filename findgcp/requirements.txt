# Python dependencies for the ArUco detection.
#
# IMPORTANT: WebODM installs this file into a per-plugin site-packages path that
# is only added to sys.path during WEB-side plugin calls (PluginBase.python_imports).
# The detection itself runs in the CELERY WORKER via eval_async, which does NOT
# enter that context — so these packages MUST also be present in the worker
# image. This file documents and pins the dependency; it does not, by itself,
# make cv2 importable in the worker. See README "Worker image requirement".
opencv-contrib-python-headless~=4.10
numpy>=1.23,<3
